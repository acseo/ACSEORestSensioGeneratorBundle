
    /**
{% block phpdoc_method_header %}
     * Update {{ entity }} entity.
{% endblock phpdoc_method_header %}
     *
     * @ApiDoc(
     *  resource=true,
     *  description="Modify a given '{{ entity }}'",
     *  statusCodes = {
     *    200 = "Returned when successful",
     *    204 = "Returned when successful modifying a '{{ entity }}'",
     *    400 = "Returned when the '{{ entity }}' being modified has validation errors",
     *    404 = "Returned when the page is not found"
     *  },
     *  parameters={
     *  //   TODO : add one line per field that is required to create a {{ entity }}
        //   {"name"="__FIELD_NAME__", "dataType"="__FIELD_TYPE__", "required"=__FIELD_BOOLEAN_REQUIRED__, "description"="__FIELD_DESCRIPTION__"}
     *  }
     * )
     * @Rest\View()     
{% block phpdoc_method_annotations %}{% endblock phpdoc_method_annotations %}
     */
{% block method_definition %}
    public function putAction({{ entity_class }} $id)
{% endblock method_definition %}
    {
{% block method_body %}
        return $this->processForm($id);
{% endblock method_body %}

{% block method_return %}
{% endblock method_return %}
    }

{% block form %}
    
{% endblock form %}
